# Заметки по интеграции звуков

## ✅ Что сделано

1. **Добавлены аудиоресурсы** в `Game.gd`:
   - Все звуковые эффекты (jump, duck, collect_egg, collect_seed, hit, game_over)
   - Фоновая музыка (bg_music.mp3)

2. **Созданы AudioStreamPlayer узлы** программно в `_init_audio()`:
   - `music_player` - для фоновой музыки
   - `sfx_player` - для звуковых эффектов

3. **Добавлены функции воспроизведения**:
   - `play_sfx()` - универсальная функция для звуков
   - `play_jump_sfx()`, `play_duck_sfx()`, `play_collect_egg_sfx()`, `play_collect_seed_sfx()` - конкретные функции

4. **Интегрированы звуки в игровые события**:
   - **Прыжок** - в `Player.gd` при вызове `jump()`
   - **Duck** - в `Player.gd` при вызове `duck()`
   - **Сбор яйца** - в `Collectible.gd` при сборе EGG
   - **Сбор семечка** - в `Collectible.gd` при сборе SEED
   - **Удар** - в `Game.gd` при `lose_life()`
   - **Game Over** - в `Game.gd` при `game_over()`

5. **Настроено управление музыкой**:
   - Автоматический запуск при старте игры
   - Пауза/возобновление при паузе игры
   - Остановка при Game Over
   - Управление через кнопки в меню паузы и Game Over

## ⚠️ Важно: Настройка зацикливания музыки

Для того, чтобы фоновая музыка зацикливалась автоматически, нужно:

1. Откройте Godot Editor
2. Выберите файл `assets/audio/music/bg_music.mp3` в FileSystem
3. В Import tab найдите настройку **Loop**
4. Установите галочку **Loop** (включить)
5. Нажмите **Reimport**

Без этой настройки музыка будет играть только один раз и остановится.

## 🔧 Дополнительные настройки (опционально)

### Изменение громкости

В `scripts/Game.gd` в функции `_init_audio()` можно изменить:

```gdscript
music_player.volume_db = -5.0  # Громкость музыки (0 = стандартная, отрицательные = тише)
sfx_player.volume_db = 0.0     # Громкость звуков (0 = стандартная)
```

### Изменение баланса музыки/звуков

Если звуки слишком тихие по сравнению с музыкой:
- Увеличьте `sfx_player.volume_db` (например, до `2.0`)
- Или уменьшите `music_player.volume_db` (например, до `-10.0`)

## 📝 Проверка работы

1. Запустите игру
2. Проверьте:
   - ✅ Музыка играет при старте
   - ✅ Звук прыжка при нажатии кнопки вверх
   - ✅ Звук duck при нажатии кнопки вниз
   - ✅ Звук сбора при сборе яйца/семечка
   - ✅ Звук удара при столкновении
   - ✅ Звук Game Over при проигрыше
   - ✅ Музыка останавливается при Game Over
   - ✅ Кнопки звука/музыки работают

## 🐛 Возможные проблемы

### Музыка не зацикливается
- Проверьте настройки импорта файла (см. выше)
- Убедитесь, что в Import tab установлен Loop

### Звуки не воспроизводятся
- Проверьте, что `sound_enabled = true` в начале игры
- Проверьте пути к файлам в `Game.gd` (должны совпадать с реальными путями)
- Проверьте, что файлы действительно загружены (размер файлов должен быть > 0)

### Музыка слишком громкая/тихая
- Измените `music_player.volume_db` в `_init_audio()`
- Или используйте баланс звука/музыки через кнопки в игре

